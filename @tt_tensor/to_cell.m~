function [tt] = to_cell(tt1)
%[TT]=TO_CELL(TT1)
%Converts TT-tensor TT1 to old-cell array 
d=tt1.d;
tt=cell(d,1);
n=tt1.n;
core=tt1.core;
pos=1;
mat=core(pos:pos+r(1)*n(1)*r(2)-1);
mat=reshape(mat,[n(1),r(1)]);

return
end